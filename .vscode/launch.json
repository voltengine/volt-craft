{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Attach (Linux)",
			"type": "cppdbg",
			"request": "attach",
			"processId": "${command:pickProcess}",
			"program": "${workspaceRoot}/cache/bin/voltcraft",
		},
		{
			"name": "Attach (Windows)",
			"type": "cppvsdbg",
			"request": "attach",
			"processId": "${command:pickProcess}"
		}
	]
}
